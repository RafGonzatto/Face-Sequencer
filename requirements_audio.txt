# Audio alignment dependencies for Face Sequencer Pro
# Install with: pip install -r requirements_audio.txt

# Core audio processing
whisperx>=3.1.0
webrtcvad>=2.0.10
librosa>=0.10.0
numpy>=1.24.0
scipy>=1.10.0

# PyTorch for WhisperX (CPU version - change for GPU if needed)
torch>=2.0.0
torchaudio>=2.0.0

# Optional: Additional audio processing
soundfile>=0.12.1
resampy>=0.4.2
matplotlib>=3.7.0  # For audio visualization

# For Portuguese text processing
unidecode>=1.3.6

# Progress tracking for long audio files  
tqdm>=4.65.0

# Note: For GPU acceleration, replace torch/torchaudio with:
# --index-url https://download.pytorch.org/whl/cu118
# torch>=2.0.0+cu118
# torchaudio>=2.0.0+cu118